# Veritas AI Frontend Environment Variables
# Leave empty for same-origin production deployment (e.g. Render full-stack)
# Or provide backend URL if hosting frontend separately (e.g. Vercel)
# VITE_API_URL=https://your-backend.onrender.com/api
VITE_API_URL=
